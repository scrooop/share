# share

Landing pages for files shared from `blood.skyron.usbx.me` (FileBrowser). Each subdirectory is a self-contained page with Open Graph metadata so links shared via iMessage / Slack / Discord render with a thumbnail.

Served via GitHub Pages at https://scrooop.github.io/share/.
